# Sunday Voice Command Assistant (VCA) - Requirements
# Install with: pip install -r requirements.txt

# Core dependencies
pyttsx3>=2.90          # Text-to-speech conversion
SpeechRecognition>=3.10.0  # Voice recognition
PyAudio>=0.2.11        # Audio I/O (microphone access)

# Web and search functionality
pywhatkit>=5.4         # YouTube automation
wikipedia>=1.4.0       # Wikipedia API wrapper
google>=3.0.0          # Google search

# Utilities
pyjokes>=0.6.0         # Random jokes

# Optional: Improve performance
# pyaudio-fork>=0.2.11  # Alternative if PyAudio installation fails

# Installation Notes:
# ==================
# 
# For Windows users:
# If PyAudio installation fails, download the wheel file:
# https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# Then install with: pip install PyAudio-0.2.11-cp39-cp39-win_amd64.whl
#
# For Linux users:
# sudo apt-get install portaudio19-dev python3-pyaudio
# pip install pyaudio
#
# For Mac users:
# brew install portaudio
# pip install pyaudio
